# {{ .ProjectName }}

基于 GoEasy DDD Lite 脚手架生成的 Go 微服务项目。

## 结构

```text
cmd/service/                    程序入口（仅装配 goeasy/app）
internal/
  domain/                       领域层（entity 行为、aggregate、domain service、event）
  app/                          应用层（application + command/ + query/）
  interface/http|grpc/          接口适配层
  infrastructure/persistence/   持久化实现
  infrastructure/client|cache|mq/
  bootstrap/                    依赖注入与路由注册
configs/
api/proto|examples|openapi/
deploy/docker|k8s/
docs/  migrations/  test/
```

## 运行

```bat
go mod tidy
go run ./cmd/service
go run ./cmd/service -f configs/config.example.yaml
```

数据库迁移（需配置 `database.enabled` 与 `database.dsn`）：

```bat
goeasy migrate up -f configs/config.yaml
make migrate-up
```

建议创建时指定 Go module：

```bat
goeasy new {{ .ProjectName }} --module github.com/yourorg/{{ .ProjectName }} --download=false
```

- 框架运行时：`goeasy/app` + `goeasy/config`
- 业务路由：`internal/bootstrap/wire.go` + `modules.go` + `register_<module>.go`（`goeasy add module/crud` 生成）
- 健康检查：`GET http://localhost:8080/health`
- 框架探针：`GET http://localhost:8080/healthz`（配置开启时）

## 模板类型

{{ .TemplateLabel }}
